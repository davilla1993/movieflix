spring:
  application:
    name: movieflix-backend
  datasource:
    username: root
    password: toor
    url: jdbc:mysql://localhost:3306/movieflix
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

project:
  poster: posters/

base:
  url: "http://localhost:8080"